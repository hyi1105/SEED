---
date: 2026-07-20
time: 10:15 (UTC+8)
tags: [product, core, checklist, seed]
status: active
---

# SEED 核心功能清單（不可少）

> 畫面不必 100% 一樣；**下列能力不可缺**。實作可換皮、換技術，但體驗要在。

## A. 知識地圖（首頁）

| 能力 | 說明 |
|---|---|
| 格子地圖 | 可設定大小（現況 10×10），有南北東西相對位置 |
| Seed 上圖 | 每個 `.md` 可擺在一格 |
| 拖曳改位 | 可拖到空格；可與他格交換 |
| 章印別名 | `alias` 最多四字，地圖上像印章；完整名用 `title` |
| 點開讀取 | 點一格 → 讀該 seed 正文 |
| 位置持久 | 可寫回倉庫（`seeds.json` 的 col/row） |

## B. 煉化／版本（阿嬤六動作）

| 能力 | 說明 | 現況 |
|---|---|---|
| 自己改 | 人手改正文 | **已有** |
| 請 AI 改 | 一句話產生建議變更 | 待做 |
| 看看改了什麼 | 舊／新對照（紅綠或等價） | **已有**（草稿差異＋Git 歷史） |
| 用這次的／不要 | 接受或丟棄建議 | **已有**（草稿） |
| 存成一版 | 文件快照，可取名 | **已有**（寫回 `.md`） |
| 回到舊的 | 選一版還原／預覽 | **已有**（可「用這版繼續改」） |
| 分享這一份 | 可給別人繼續煉的 seed | 部分（Public repo／連結） |

## C. 內容與協作原則

| 能力 | 說明 |
|---|---|
| Markdown 為本 | 筆記是 `.md`，人可掃、AI 可改 |
| 只放可公開學習筆記 | Public 策略；敏感資料不進庫 |
| 用語藏工程 | 介面不說 commit／push／branch |
| 與 Cursor 共維 | Agent 改筆記 → 人看 diff 確認 |

## D. 刻意不算核心（可晚做）

- 多來源自動抓取、法務判決即時、多人即時協作、OAuth、手機原生 App、真經緯度地圖

## 判定標準

重做或換實作時：若 **A 全有**，且 **B** 除「請 AI 改／完整 seed 包分享」外皆可用，即核心不缺。畫面長相不列入判定。

**2026-07-20**：已接上自己改、草稿用／不要、存成一版（寫回 md）。下一刀：請 AI 改。
